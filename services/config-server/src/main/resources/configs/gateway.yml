server:
  port: 8888
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: authentification
          uri: lb:http://AUTHENTIFICATION
          predicates:
            - Path=/api/auth/**
  main:
    allow-bean-definition-overriding: true